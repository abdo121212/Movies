import React, { createContext } from "react";
const colorContext = createContext();

export default function Mina() {
  return <colorContext.Provider value='white'>
  
  
  
  
  </colorContext.Provider>;
}
